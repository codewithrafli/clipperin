# Redis Configuration
REDIS_URL=redis://redis:6379/0

# Data Directory (inside container)
DATA_DIR=/data

# Whisper Model (tiny, base, small, medium, large)
WHISPER_MODEL=base

# Whisper Language (optional - leave empty for auto-detect)
# Common: en (English), id (Indonesian), ms (Malay), es (Spanish), etc.
# WHISPER_LANGUAGE=id

# Video Output Settings
OUTPUT_WIDTH=1080
OUTPUT_HEIGHT=1920
OUTPUT_FPS=25
OUTPUT_CRF=23

# Clip Duration (seconds)
CLIP_START=30
CLIP_DURATION=30

# AI API Keys (OPTIONAL - for enhanced smart clip detection)
# Leave empty to use free rule-based detection
# Uncomment and add your key for AI-powered detection
# GEMINI_API_KEY=your_gemini_api_key_here
# OPENAI_API_KEY=your_openai_api_key_here
