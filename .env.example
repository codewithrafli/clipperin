# Redis Configuration
REDIS_URL=redis://redis:6379/0

# Data Directory (inside container)
DATA_DIR=/data

# Whisper Model (tiny, base, small, medium, large)
WHISPER_MODEL=base

# Whisper Language (optional - leave empty for auto-detect)
# Common: en (English), id (Indonesian), ms (Malay), es (Spanish), etc.
# WHISPER_LANGUAGE=id

# Video Output Settings
OUTPUT_WIDTH=1080
OUTPUT_HEIGHT=1920
OUTPUT_FPS=25
OUTPUT_CRF=23

# Clip Duration (seconds)
CLIP_START=30
CLIP_DURATION=30

# ===========================================
# AI PROVIDER SETTINGS
# ===========================================
# Choose your AI provider: "gemini", "groq", "openai", or "none"
# "none" = free rule-based detection (no AI needed)
AI_PROVIDER=groq

# API Keys (only need ONE based on your chosen provider)
# Get Groq key at: https://console.groq.com ($10 free credit, no CC needed!)
# Get Gemini key at: https://aistudio.google.com/app/apikey (FREE tier)
# Get OpenAI key at: https://platform.openai.com/api-keys
GROQ_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=

# ===========================================
# AI FEATURES (NEW!)
# ===========================================
# Auto Hook - Generate viral intro text overlay
ENABLE_AUTO_HOOK=false
HOOK_DURATION=5
HOOK_STYLE=bold

# Smart Reframe - Track speaker face, keep centered (FREE - uses OpenCV)
ENABLE_SMART_REFRAME=false
REFRAME_SMOOTHING=0.15

# ===========================================
# PROGRESS BAR
# ===========================================
ENABLE_PROGRESS_BAR=true
PROGRESS_BAR_COLOR=#FF0050
PROGRESS_BAR_HEIGHT=6
PROGRESS_BAR_POSITION=bottom

# ===========================================
# ASPECT RATIO
# ===========================================
# Options: "9:16" (TikTok/Reels), "1:1" (IG Square), "4:5" (IG/FB)
OUTPUT_ASPECT_RATIO=9:16
